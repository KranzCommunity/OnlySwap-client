import { SearchInLoopPipe } from './search-in-loop.pipe';

describe('SearchInLoopPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchInLoopPipe();
    expect(pipe).toBeTruthy();
  });
});
